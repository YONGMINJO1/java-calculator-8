package calculator;

public class StringAddCalculator {

    public static int add(String input) {
        // 기능
        if (input == null || input.isEmpty()) {
            return 0;
        }
        return 0;
    }
}
